name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # 将 'main' 替换为默认分支名称（例如 'master'）

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Fetch all history
        run: git fetch --prune --unshallow

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: Install dependencies
        run: npm ci

      - name: Build Vue 3 TypeScript Vite project
        run: npm run build

      - name: Build Vue 3 TypeScript Vite project
        run: npm -v

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: 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
          publish_dir: ./dist 
